if species == "elf":
	if randomNumber == 1:
		tempPathAudio = load()
	else:
		tempPathAudio = load()
elif species == "ork":
	if randomNumber == 1:
		tempPathAudio = load()
	else:
		tempPathAudio = load()
elif species == "dwarfs":
	if randomNumber == 1:
		tempPathAudio = load()
	else:
		tempPathAudio = load()